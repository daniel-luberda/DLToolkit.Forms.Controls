<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	x:Class="Examples.ExamplesFlowListView.Views.ThirdTemplateXaml" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">

    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

	<Grid.Children>

		<Label HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
			XAlign="End" YAlign="Center" Text="{Binding Title}" Grid.Row="0" Grid.Column="0"/>

		<BoxView Color="Gray" Grid.Row="0" Grid.Column="1"/>

	</Grid.Children>

</Grid>
